@using Indigo.Organization;
@model Indigo.WebMatrix.Models.PositionModels.AddModel

<div class="row">
    <div class="col-xs-12 col-sm-6 col-md-4">
        <div class="box">
            @using (Html.BeginForm("Add", "Position", FormMethod.Post, new { role = "form" }))
            {
                @Html.AntiForgeryToken()
                <div class="box-body">
                    <div class="form-group has-feedback">
                        @Html.LabelFor(m => m.Name)
                        @Html.TextBoxFor(m => m.Name, new { @class = "form-control", autofocus = true })
                        @Html.ValidationMessageFor(m => m.Name)
                    </div>
                    <div class="form-group has-feedback">
                        @Html.LabelFor(m => m.Rank)
                        @Html.TextBoxFor(m => m.Rank, new { @class = "form-control" })
                        @Html.ValidationMessageFor(m => m.Rank)
                    </div>
                </div>
                <div class="box-footer">
                    @Html.ActionLinkAuthorized("返回", "Index", null, new { @class = "btn btn-default" })
                    <button type="submit" class="btn btn-primary">保存</button>
                </div>
            }
        </div>
    </div>
</div>
